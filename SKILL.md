---
name: "ambiguity-detector"
description: "检测用户需求中的歧义，当功能归属不明、同名冲突、术语多义或需求模糊时主动确认。支持任务前预检和执行中动态检测，适用于任意前端/后端项目。"
---

# 通用歧义检测 Skill（核心指令版）

> **设计原则**: 本文件为核心指令（~5KB），详细参考数据已拆分至 `references/` 目录按需查阅。
>
> **Token 优化**: 优先读取预生成的 `index.json` 索引文件（由 `scripts/build-index.mjs` 生成），避免实时扫描文件系统消耗 token。
>
> **双阶段检测**: 不仅在任务开始前预检（Pre-flight），还在**任务执行过程中动态检测**（In-flight），覆盖"做到一半才发现有歧义"的真实场景。

## 1. 核心定位

在**任何类型的项目**中，当以下情况出现时触发：
- 功能/资源位置不明确
- 存在多个同名或相似名称的匹配项
- 同一术语在不同上下文有不同含义
- 需求缺少关键信息（WHAT / WHERE / HOW）
- **开发过程中新发现的同名/类似实现、引用歧义、影响范围不确定**

**目标**: 全流程澄清歧义——**开始前防患于未然，过程中及时纠偏**，避免错误开发、返工和浪费时间。

## 2. 工作流程（双阶段）

### 阶段一：Pre-flight（任务开始前）

```
用户输入 → 解析关键词 → [读 index.json 或 实时扫描] → 匹配评估 → 歧义判定
  ├─ 无歧义 → ✅ 确认目标 → 进入开发
  └─ 有歧义 → ⚠️ 生成报告 → 用户选择 → 记录结果 → 进入开发
```

### 阶段二：In-flight（任务执行中）

```
开发进行中 → 遇到可疑信号 → 快速交叉验证 → 发现歧义？
  ├─ 无歧义 → 继续当前任务
  └─ 有歧义 → ⏸️ 暂停当前任务 → 🔔 中途歧义报告 → 用户决策
       ├─ 选择 A → 继续原方向（记录决策依据）
       ├─ 选择 B → 切换到正确目标（说明变更内容）
       └─ 选择 C → 回退已做的改动（说明回退范围）
```

### Step 0: 检查索引缓存（Token 节省关键）

**优先级最高**: 先检查 `.trae/skills/ambiguity-detector/index.json` 是否存在且新鲜（对比 `meta.generatedAt`）。

- **存在且新鲜** → 直接读取 `filesByName` 做匹配，跳过文件系统扫描
- **不存在或过期** → 回退到实时扫描模式，并在报告中提示可运行 `scripts/build-index.mjs` 生成索引

### Step 1: 快速判断是否需要触发

**直接跳过（不触发）**:
- 输入包含精确文件路径（有 `/` 或 `\` 分隔符的路径）
- 纯操作指令（运行测试、拉取代码、查看日志、提交代码等）

**必须触发**:
- 提到功能/资源名但无路径
- 使用模糊词：优化、调整、改一下、加一个、那个、好像、大概
- 需求明显缺少关键信息

### Step 2: 项目识别与搜索

#### 2.1 识别项目类型

快速检查根目录特征文件判断项目类型。**详细指纹规则见 `references/fingerprints.md`**。

速查表：

| 特征 | 类型 |
|------|------|
| `package.json` + `.vue` | Vue 前端 |
| `package.json` + `jsx/tsx` | React 前端 |
| `angular.json` | Angular |
| `pom.xml` / `build.gradle` | Java 后端 |
| `go.mod` | Go |
| `requirements.txt` / `pyproject.toml` | Python |
| `Cargo.toml` | Rust |
| `composer.json` | PHP |
| `pnpm-workspace.yaml` / `lerna.json` / `turbo.json` | **Monorepo**（需先确定子项目范围） |

#### 2.2 执行匹配

根据关键词在项目中搜索匹配项。搜索策略按项目类型动态选择：

**前端优先搜索**: views/, pages/, components/, router/, stores/, api/
**后端优先搜索**: controllers/, services/, models/, repositories/, routes/
**Monorepo**: 先确定子项目列表，再在各子项目中分别搜索

### Step 3: 歧义评估

| 匹配数 | 判定 | 处理 |
|--------|------|------|
| 0 | 可能是新功能 / 关键词过宽泛 | 询问用户确认范围或换关键词 |
| 1 | 低风险 | 可直接确认进入开发 |
| ≥2 | **高风险** | 必须生成报告让用户选择 |

### Step 4: 四种歧义场景处理

#### 场景 A: 归属不明
用户提到功能但未指定位置，搜索到多个候选。

**处理**: 列出候选（路径+说明），请用户选择。

```
用户: "添加搜索功能"
→ 列出所有 search 相关位置（组件/页面/API），问用户选哪个
```

#### 场景 B: 同名冲突
同一名称的资源在项目中存在多个匹配。

**处理**: 列出所有同名资源（完整路径+类型标注），请用户选择。

```
用户: "修改登录逻辑"
→ 列出: 登录页.vue / login API / LoginController / auth工具函数
```

#### 场景 C: 术语多义
同一术语在不同上下文有不同含义。

**处理**: 说明可能的含义，请用户指明业务场景。

```
用户: "修改订单状态"
→ 可能是: 数据库字段 / 状态机规则 / API枚举 / 前端展示
```

#### 场景 D: 需求模糊
缺少 WHAT/WHERE/HOW 关键信息。

**处理**: 针对缺失维度追问。

```
用户: "优化一下列表"
→ 追问: 哪个列表？什么性能问题？前后端？数据量？
```

**详细示例见 `references/examples.md`**（23 个覆盖各场景的完整示例）。

### Step 6: In-flight 动态歧义检测（执行中）

> **核心场景**: 任务已经启动并开始编写/修改代码，但在过程中发现新的歧义。这是实际开发中最常见的歧义来源。

#### 6.1 何时触发中途检测

在开发过程中，遇到以下**可疑信号**时必须主动触发交叉验证：

| 可疑信号 | 触发条件 | 典型案例 |
|---------|---------|---------|
| **同名文件发现** | 修改 A 文件时，搜索发现同名或高度相似的 B 文件 | 改 `UserList.vue` 时发现 `user-list.tsx` 也存在 |
| **多版本引用** | 当前修改的资源被多个不同版本/位置引用 | 改了一个 `utils/format.ts`，发现 `shared/format.js` 功能重复 |
| **Import/依赖链分叉** | 追踪 import 链时发现同一功能有多条路径 | 组件引用了 store A，但页面还直接引用了 store B |
| **重构碰撞风险** | 重命名/移动/删除操作可能影响其他未预期的位置 | 删除一个函数，发现有 3 个其他文件在调用 |
| **配置/常量重复** | 发现多处定义了相同含义的配置或常量 | 三个文件各自定义了 `API_BASE_URL` |
| **接口签名不一致** | 调用方和实现方的参数/返回值不匹配 | 前端传 `userId` 但后端接收的是 `id` |
| **类型定义冲突** | TypeScript 接口/类型在不同文件中有不同定义 | `User` 类型在 3 个文件中字段各不相同 |

#### 6.2 中途检测流程

```
遇到可疑信号
    ↓
[快速验证] 用 index.json 或 Grep 做轻量级交叉搜索（≤2次查询）
    ↓
┌─ 结果唯一 / 无冲突 → ✅ 记录为"已验证无歧义"，继续任务
│
└─ 结果有多个匹配 / 存在冲突 → ⏸️ 暂停当前任务
                                   ↓
                              🔔 输出中途歧义报告
                                   ↓
                              等待用户决策
                                   ↓
                    ┌──────────────┼──────────────┐
                    ↓              ↓              ↓
               [继续原方向]    [切换目标]     [回退改动]
```

#### 6.3 中途歧义报告格式

```markdown
## 🔔 执行中发现歧义（In-flight）

**当前任务**: {正在做什么的简要描述}

**触发信号**: {哪个可疑信号触发了本次检测}

**已完成的改动**: {列出已修改/新增的文件和变更摘要}

**发现的歧义**:
- **类型**: {同名冲突 / 引用分叉 / 重构风险 / ...}
- **选项 A**: `{路径}` - {说明} ← 当前正在操作的
- **选项 B**: `{路径}` - {说明} ← 新发现的候选
- （更多选项...）

**影响评估**:
- 如果选 A（继续当前方向）: {对选项B是否有影响？}
- 如果选 B（切换到新目标）: {需要回退/调整哪些已完成的工作？}

**需要您确认**: {具体问题}
```

#### 6.4 用户决策后的处理

**决策 A: 继续原方向**
- 在报告中记录："经确认，选择 {A}，{B} 为独立模块/历史遗留/其他用途"
- 继续完成剩余任务
- 在最终交付说明中提及此决策

**决策 B: 切换到正确目标**
- 说明从 A 切换到 B 的原因
- 回退或调整已完成的与 A 相关的改动
- 以 B 为目标继续后续工作

**决策 C: 回退所有改动**
- 列出需要回退的文件清单
- 执行回退操作
- 等待用户进一步指示

#### 6.5 中途检测的最佳实践

1. **早发现早报告**: 一旦产生怀疑立即验证，不要等到任务快结束时
2. **轻量验证优先**: 先用 1-2 次 Grep/index.json 查询确认是否真有歧义，避免过度中断
3. **保留上下文**: 报告中要包含"已完成的改动"，方便用户判断切换成本
4. **不要自动回退**: 除非用户明确要求，否则保留已完成的工作
5. **记录决策**: 无论哪种结果，都记录下来避免后续再次产生同样疑问

### Step 5: 输出报告

#### 有歧义时

```markdown
## ⚠️ 需求歧义检测报告

**原始需求**: {回显用户输入}

**项目类型**: {自动识别的类型}

**检测到的歧义类型**: {A/B/C/D}

**候选选项**:
1. **{名称}** - `{相对路径}` - {一句话说明}
2. **{名称}** - `{相对路径}` - {一句话说明}

**需要您确认**: {具体问题}

💡 **推荐依据**: {基于上下文的建议}
```

#### 无歧义时

```markdown
✅ 需求已确认，无歧义检测到。

**确认的目标**: {确定的路径}

**可以开始执行**: {下一步建议}
```

## 3. 别名匹配

为提升中文输入的匹配率，内置常见功能的中英文别名映射。

**完整别名表见 `references/aliases.md`**（13 个分类，200+ 术语）。

常用速查：

| 中文 | 英文别名 |
|------|---------|
| 登录 | login, signin, auth, 认证 |
| 搜索 | search, query, find, 查找 |
| 列表 | list, table, grid |
| 新增 | add, create, new |
| 编辑 | edit, update, modify |
| 删除 | delete, remove, del |
| 导出 | export, download |
| 导入 | import, upload |
| 设置 | setting, config |
| 接口 | api, interface, endpoint |
| 组件 | component |
| 弹窗 | dialog, modal, popup |

匹配规则: 不区分大小写 → 完全匹配 > 前缀匹配 > 包含匹配。

## 4. 与其他 Skill 协作

本 Skill 提供**双阶段守卫**能力：

```
需求进入 → [Pre-flight 歧义检测] → [其他 Skill 实际开发] → [In-flight 动态检测（过程中发现歧义时）]
           ↑ 阶段一：开始前              ↑ 阶段二：执行中
```

**阶段一 Pre-flight**: 在任何开发类 Skill **之前**调用，确保方向正确
**阶段二 In-flight**: 在开发 Skill **执行过程中**按需触发，及时纠偏

协作原则：
- 只负责歧义检测，不参与代码修改
- 检测结果传递给下游 Skill，避免重复询问
- In-flight 检测应**轻量快速**，避免过度中断开发流程
- 如果下游 Skill 已具备上下文感知能力，可选择性跳过 Pre-flight

## 5. 异常降级

| 异常 | 处理方式 |
|------|---------|
| 无法识别项目类型 | 使用通用全项目扫描，报告中标注提示 |
| 搜索超时/失败 | 返回已有结果，展示目录结构供用户手动指定 |
| 匹配结果过多(>10) | 截断 top 10，提示"还有 N 个，回复查看更多" |
| index.json 不存在/过期 | 回退到实时扫描，提示可运行脚本生成索引 |

## 6. 参考文件索引

| 文件 | 用途 | 何时查阅 |
|------|------|---------|
| `references/fingerprints.md` | 全量项目指纹表（9 大类工程类型） | 遇到不常见的项目类型时 |
| `references/aliases.md` | 完整别名映射（200+ 术语） | 默认别名未命中时扩展匹配 |
| `references/examples.md` | 详细示例库（23 个 Pre-flight 场景 + In-flight 场景） | 遇到复杂/边缘案例时参考输出格式 |
| `config.json` | 自定义配置（排除目录/别名/模块分组） | 需要自定义规则时 |
| `scripts/build-index.mjs` | 索引生成脚本 | 首次使用或项目结构变更后执行 |
